import reducer from './settings.reducer';
import * as selectors from './settings.selectors';
import * as actions from './settings.actions';

export default { name: 'settings', reducer, selectors, actions };
