import reducer from './rastertiles.reducer';
import * as selectors from './rastertiles.selectors';
import * as actions from './rastertiles.actions';

export default { name: "rastertiles", reducer, selectors, actions };
